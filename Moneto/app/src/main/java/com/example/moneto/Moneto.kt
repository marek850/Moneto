package com.example.moneto

/*
object Moneto {
    lateinit var db: AppDatabase
        private set

    val repository by lazy {
        Repository(transactionDao = db.transactionDao())
    }
    fun provide(context: Context){
        db = AppDatabase.getDatabase(context)
    }
}*/
